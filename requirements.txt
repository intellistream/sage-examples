# Requirements for SAGE Examples
#
# ⚠️ DEPRECATED: This file is kept for backward compatibility only.
# Prefer using: pip install -e .
#
# Installation:
#   pip install -r requirements.txt
#
# For development:
#   pip install -e .[dev]

# === SAGE Core Packages (from PyPI) ===
# All dependencies are declared in pyproject.toml
# Installing this package will automatically install:
# - isage-common>=0.2.0
# - isage-llm-core>=0.2.0
# - isage-libs>=0.2.0
# - isage-apps>=0.2.0
# - All application dependencies (torch, opencv, etc.)

# To install from pyproject.toml:
-e .

# For development mode (includes pytest, ruff, mypy):
# pip install -e .[dev]
